import React from 'react';
import { Icon } from 'antd';

// 确认框禁用问号图标
export const Question_Icon = (<Icon type="question-circle-o" style={{ color: 'var(--primary-color)' }} />);

export const X_Icon = (<Icon type="question-circle-o" style={{ color: 'red' }} />);
